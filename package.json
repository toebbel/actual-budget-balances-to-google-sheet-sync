{
  "dependencies": {
    "@actual-app/api": "^25.8.0",
    "@google-cloud/local-auth": "3.0.1",
    "cmd-ts": "^0.13.0",
    "date-fns": "^4.1.0",
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "full-sync": "rm -rf HaTo-* && npx ts-node index.ts",
    "quick-sync": "rm -rf HaTo-* && npx ts-node index.ts --no-bank-sync"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  }
}
